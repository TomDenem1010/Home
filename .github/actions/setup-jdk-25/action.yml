name: Set up JDK 25
description: Configures Temurin JDK 25 and the Maven dependency cache.

runs:
    using: composite
    steps:
        - name: Set up JDK 25
          uses: actions/setup-java@v5
          with:
              distribution: temurin
              java-version: "25"
              cache: maven
