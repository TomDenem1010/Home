name: Checkout repository
description: Checks out the selected Git revision.

inputs:
    ref:
        description: Git reference to check out.
        required: false
        default: main

runs:
    using: composite
    steps:
        - name: Checkout repository
          uses: actions/checkout@v5
          with:
              ref: ${{ inputs.ref }}
